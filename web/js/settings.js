/* SETTINGS */

// This is the point at which in the coordinate system where the text will be reversed when zooming in in order to prevent text going off the sizes of the radar.
// This point will depend on how long the technology names are.
const DangerZoneInvert = 15;

// Values setting the origin of the coordinate system, set in percentage points, starting from the bottom left.
const xOffset = 50;
const yOffset = 10;

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

export { DangerZoneInvert, xOffset, yOffset };